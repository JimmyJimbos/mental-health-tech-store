# Optional: copy to .env.local to get AI-written listing titles/descriptions
# on the /listing page. Without it, the app falls back to a simple template —
# it still produces a usable listing, just less polished.
ANTHROPIC_API_KEY=
